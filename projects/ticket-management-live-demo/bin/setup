#!/usr/bin/env ruby
require "fileutils"

system("bundle install") || abort("bundle install failed")
system("bin/rails db:prepare") || abort("database setup failed")
puts "Ready. Start the server with bin/rails server."
